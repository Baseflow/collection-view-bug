<?xml version="1.0" encoding="UTF-8"?>
<Frame
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:views="clr-namespace:MyXamarinFormsApp.UI.Controls"
    x:Class="MyXamarinFormsApp.UI.Controls.IconButton"
    x:Name="root">
    <Frame.GestureRecognizers>
        <TapGestureRecognizer Command="{Binding Command, Source={x:Reference root}}" CommandParameter="{Binding CommandParameter, Source={x:Reference root}}" />
    </Frame.GestureRecognizers>
    <views:IconLabel
        x:Name="IconLabel"
        VerticalOptions="Center"
        HorizontalOptions="Center"
        IconPosition="{Binding IconPosition, Source={x:Reference root}}"
        IconSource="{Binding IconSource, Source={x:Reference root}}"
        IconColor="{Binding IconColor, Source={x:Reference root}}"
        IconSize="{Binding IconSize, Source={x:Reference root}}"
        Text="{Binding Text, Source={x:Reference root}}"
        Spacing="{Binding Spacing, Source={x:Reference root}}"
        TextColor="{Binding TextColor, Source={x:Reference root}}"
        FontFamily="{Binding FontFamily, Source={x:Reference root}}"
        FontSize="{Binding FontSize, Source={x:Reference root}}" />
</Frame>
